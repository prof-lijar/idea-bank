import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // When you deploy, consider `headers()` for HSTS, X-Frame-Options, etc. (hosting-specific).
};

export default nextConfig;
